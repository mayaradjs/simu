#include <stdio.h>
#include <stdlib.h>

typedef struct dadosCliente{

	unsigned int cod, operacao, valor;
	
} dadosCliente;



int main(){

	unsigned int n, m;

	scanf ("%u %u", &m, &n);

	dadosCliente filaDeAtendimento[n];	

	for (int i=0; i<n; i++){
		scanf("%u %u %u", &cliente.cod, &cliente.operacao, &cliente.valor);
		
		

	}
	


return 0;
}
